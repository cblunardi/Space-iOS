import Foundation

typealias UIC = UIConstants
struct UIConstants {
    private init() {}

    typealias Anims = Animations
    struct Animations {
        private init() {}

        static let defaultDuration: TimeInterval = 0.2
    }
}
